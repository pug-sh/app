# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Proto symlink (points to pug backend)
proto

# Map assets wired in for local dev by `bun run sync:app` in the pug-maps repo (../maps).
# The basemap and fonts are symlinks into that repo; the POV data is copied. Every deployed
# environment loads these from the CDN instead, so none of it belongs in the tree.
public/basemap-*.pmtiles
public/fonts
public/pov/

# Generated proto code (regenerate with: bun run generate)
# src/api/genproto

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.playwright-mcp/
.superpowers/
docs/superpowers/
.claude/worktrees/

# Qwen settings
.qwen/settings.json

# PR review notes
pr-*review.md
pr-review.md
/*.png
# Local env files (.env.example is committed as the template)
.env.development
.env.production
.env